import Layout from '../../hooks/layout/Layout';

const Home: React.FC = ({}) => {
    return (
        <Layout>
          <div className="container my-1">
            <p>Home</p>
          </div>  
        </Layout>
    );
};

export default Home;